# Tests

Run with `bash tests/run.sh`. Uses Node ≥20 built-in test runner (`node:test`). Zero dependencies. Covers `bootstrap.test.js` (SessionStart installer/validator); `fixtures/stale-subagent-stop.js` is a bootstrap fixture.
